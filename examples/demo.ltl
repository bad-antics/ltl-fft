// examples/demo.ltl — runnable demo for ltl_fft

import std::io
use ltl_fft as lib

fn main() {
    io::println("ltl_fft demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}